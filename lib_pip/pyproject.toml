[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nexpyrs_shared"
version = "0.1.0"
authors = [
  { name="NexPyRS Team" },
]
description = "Shared Python utilities for NexPyRS microservices"
requires-python = ">=3.8"
dependencies = []
